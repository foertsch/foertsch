name: Metrics
on:
  schedule: [{cron: "0 4 * * 1"}]
  push: {branches: ["main"]}
  workflow_dispatch:
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GH_METRICS }}
          committer_token: ${{ secrets.GITHUB_TOKEN }}
          filename: metrics.svg
          user: foertsch
          template: classic
          base: activity, community, repositories, metadata
          config_animations: yes
          config_timezone: Europe/Zurich

          plugin_isocalendar: yes

          plugin_languages: yes
          plugin_languages_ignored: tex, less, dockerfile, makefile, shell, cmake

          plugin_calendar: yes
          plugin_calendar_limit: 1

          # plugin_habits disabled: crashes on events with null commit author
          # (known lowlighter/metrics bug). Re-enable when fixed upstream.
          plugin_habits: no
